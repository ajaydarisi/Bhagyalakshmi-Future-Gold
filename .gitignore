# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
bfg-design-system
# dependencies
/node_modules
/public/vad/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but the voice service's template must be committed: common/env.ts and
# docs/voice-assistant-production.md both tell you to copy it, so an ignored
# example makes the first-run error message a dead end. Contains no values.
!voice-agent/.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Firebase config (generated from env vars)
android/app/google-services.json

# Capacitor native build artifacts
android/app/build/
android/.gradle/
ios/App/Pods/
ios/App/build/

context.md
playwright-report
test-results
design_handoff_mobile_storefront
.playwright-mcp
